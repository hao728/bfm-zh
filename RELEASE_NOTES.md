# v1.2.1-zh.3 正式版

## 发布标题
**BFM 中文美化版 v1.2.1-zh.3 — 智能图标 + 压缩管理 + 稳定优化**

## 简介
基于 Banner File Manager v1.2.1 的中文增强版本，专为 Winlator / Wine 桌面环境优化。本次更新重点完善文件类型智能识别图标、新增压缩包管理功能，并修复了多项稳定性问题。

---

## 更新日志

### 新增功能

**文件类型智能识别图标**
- 图片文件自动生成缩略图预览
- 音乐、视频、压缩包、文档、表格、演示等格式均有独特 GDI 绘制图标
- 新增脚本文件（py/js/sh/vbs/ps1/lua）和配置文件（ini/cfg/json/xml/yaml）图标
- 格式覆盖扩展：图片+ tiff/svg，音乐+ ape/wv/opus/mid，视频+ mpg/rmvb/ts/3gp，文档+ odt/epub

**exe 图标增强**
- 使用 ExtractIconEx 直接从 exe 提取图标，比 SHGetFileInfo 更可靠
- 提取失败自动查找同目录 .ico 文件，解决特殊游戏图标丢失问题
- 图标缓存机制，避免重复提取

**压缩包管理（需 7-Zip）**
- 解压到当前文件夹 / 解压到同名文件夹
- 测试压缩包完整性（7z t）
- 压缩为 ZIP / 7Z（支持文件、文件夹、多选）
- 解压/压缩进度窗口实时显示

### 改进优化

- 图标三级缓存（缩略图+自定义图标+exe图标），大幅提升滚动流畅度
- 大图标视图文件名自动换行，不再截断
- 详细信息视图列宽优化，日期/类型/大小显示完整
- 右键菜单重新整理，压缩相关功能合并分组
- 字体渲染优化，标题栏和右键菜单更清晰
- 视图设置、收藏夹、字体大小等配置持久化保存

### 问题修复

- 修复大图标视图 exe 文件图标不显示的问题
- 修复切换视图或加载大目录时偶发崩溃
- 修复文件名过长导致的显示截断
- 修复解压菜单文本与实际功能不符的问题
- 修复多个构建错误（前向声明、变量作用域、链接符号）
- 修复小图标/列表/详细信息视图图标不显示（根因：缓存位图被误删 + 白色背景未透明化）
- 修复自定义图标缓存满后图标丢失（改为环形覆盖最旧条目）
- 修复预览窗格 exe 文件无图标（改用 ExtractIconEx 增强提取）
- 修复解压/压缩完成后无结果提示（新增成功/失败弹窗，含退出码）

### 新增排序功能

- 空白区域右键 → 排序方式 → 按名称/类型/大小/日期
- 所有视图通用（大图标/小图标/列表/详细信息）
- 再次点击切换升序/降序，设置自动持久化

---

## 安装说明

### 自动安装（推荐）
1. 下载 `bfm-zh-x64.zip`
2. 解压后运行 `安装.bat`
3. 脚本自动完成备份、安装、重启

### 手动安装
1. 备份原 `wfm.exe`
2. 将 `wfm.exe` 和 `libcdio.dll` 复制到 WFM 目录覆盖

### 7-Zip 部署
- 自动安装脚本会检测并部署
- 手动：将 `7z.exe` + `7z.dll` 放入 `Z:\opt\apps\7-Zip\`
- 推荐版本：7-Zip 26.03（[官方下载](https://www.7-zip.org/)）

---

## 已知问题

- 转区启动为实验性功能，可能与容器 `LC_ALL` 编码设置冲突
- 自适应窗口/全屏参数对部分游戏无效
- 加速启动效果因游戏而异，激进模式可能导致不稳定
- 预览窗格暂不支持视频/音频播放，仅显示图标

---

## 文件清单

| 文件 | 说明 |
|------|------|
| wfm.exe | 主程序 |
| libcdio.dll | 光盘镜像支持库 |
| 安装.bat | 自动安装脚本 |
| 卸载还原.bat | 卸载还原脚本 |
| README.md | 项目说明 |

---

## 校验信息

- 基于上游：The412Banner/banner-file-manager v1.2.1
- 协议：GPL-3.0-or-later
- 目标平台：Winlator / Wine (x86_64)
